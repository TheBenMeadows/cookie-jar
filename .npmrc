# Do not auto-install peer dependencies. The wallet adapter declares React Native as an
# optional peer for its mobile-wallet support, which this app does not use; auto-installing it
# pulls the whole React Native toolchain (metro and its own dependencies) into the tree, where
# it is never built, bundled or run. Without this flag every `npm install` puts it back.
legacy-peer-deps=true
